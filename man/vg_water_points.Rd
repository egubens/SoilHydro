% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentation.R
\name{vg_water_points}
\alias{vg_water_points}
\title{FC, PWP, and available water from VG parameters (kPa)}
\usage{
vg_water_points(
  params_df,
  id_col = NULL,
  fc_kPa = 10,
  pwp_kPa = 1500,
  filter_id = NULL
)
}
\arguments{
\item{params_df}{Output of \code{\link{vg_fit_optim}}.}

\item{id_col}{Name of the ID column in \code{params_df}. Defaults to the first column.}

\item{fc_kPa}{Numeric, suction for FC (default 10 kPa).}

\item{pwp_kPa}{Numeric, suction for PWP (default 1500 kPa).}

\item{filter_id}{Optional scalar to compute \strong{only one product} from \code{params_df}.}
}
\value{
\code{data.frame} with columns \code{ID, theta_fc, theta_pwp, AWC}.
}
\description{
Computes \eqn{\theta} at FC suction (default 10 kPa),
at permanent wilting point (default 1500 kPa), and returns available water content.
}
\examples{
# Using 'fits' from vg_fit_optim():
# Compute FC at 10 kPa and PWP at 1500 kPa plus AWC:
wp <- vg_water_points(fits, id_col = "ID", fc_kPa = 10, pwp_kPa = 1500)
wp

# Change the FC definition (e.g., 30 kPa):
wp30 <- vg_water_points(fits, id_col = "ID", fc_kPa = 30, pwp_kPa = 1500)
wp30
}
