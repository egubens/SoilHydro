% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentation.R
\name{vg_predict}
\alias{vg_predict}
\title{Predict \eqn{\theta(h)} for new suctions (kPa or hPa)}
\usage{
vg_predict(
  params_df,
  id_col = NULL,
  new_h,
  units = c("kPa", "hPa"),
  filter_id = NULL
)
}
\arguments{
\item{params_df}{Output of \code{\link{vg_fit_optim}}.}

\item{id_col}{Name of the ID column in \code{params_df}. Defaults to the first column.}

\item{new_h}{Numeric vector of new suctions.}

\item{units}{Units of \code{new_h}, either \code{"kPa"} or \code{"hPa"}.}

\item{filter_id}{Optional scalar to return predictions only for a single ID.}
}
\value{
\code{data.frame} with columns \code{ID, h, units, theta}.
}
\description{
Generates predictions from fitted VG parameters at user-supplied
suctions expressed in either kPa or hPa.
}
\examples{
# Using 'fits' from the vg_fit_optim() example:
# Predict theta at custom suctions (kPa) for all IDs:
new_grid <- c(0.5, 5, 50, 500, 1500)
pred_all <- vg_predict(fits, id_col = "ID", new_h = new_grid, units = "kPa")
head(pred_all)

# Predict only for a single ID:
pred_A <- vg_predict(fits, id_col = "ID", new_h = new_grid, units = "kPa", filter_id = "A")
pred_A

# If your target grid is in hPa, set units = "hPa":
new_hPa <- c(10, 100, 1000, 10000)  # equals 1, 10, 100, 1000 kPa
pred_hPa <- vg_predict(fits, id_col = "ID", new_h = new_hPa, units = "hPa")
head(pred_hPa)
}
