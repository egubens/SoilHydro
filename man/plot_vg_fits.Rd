% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentation.R
\name{plot_vg_fits}
\alias{plot_vg_fits}
\title{Plot observed WRC and fitted VG curve per treatment}
\usage{
plot_vg_fits(
  data,
  params_df,
  id = NULL,
  id_value = NULL,
  theta,
  h,
  units = c("kPa", "hPa"),
  log_x = TRUE,
  points = 400
)
}
\arguments{
\item{data}{Original data.frame used for fitting.}

\item{params_df}{Output of \code{\link{vg_fit_optim}}.}

\item{id, theta, h}{Character names of the ID, theta, and suction columns in \code{data}.
Use \code{id=NULL} for a single-group dataset.}

\item{id_value}{Optional scalar to plot \strong{only one product} (rows where \code{data[[id]] == id_value}).
Ignored if \code{id} is \code{NULL}.}

\item{units}{Units of the suction column in \code{data}: "kPa" or "hPa".}

\item{log_x}{Logical; draw x-axis on log10 scale.}

\item{points}{Integer; number of points for the smooth VG curve.}
}
\value{
Named list of ggplot objects, one per ID (or a single plot if only one ID).
}
\description{
Produces one ggplot per treatment/ID, overlaying observed points
with the fitted Van Genuchten curve. Uses tidy-eval (no aes_string).
}
\examples{
## Plot observed vs fitted curves per ID
## Not run on CRAN to keep checks fast.
\dontrun{
  library(ggplot2)
  p_list <- plot_vg_fits(
    data      = df,          # from vg_fit_optim() example
    params_df = fits,
    id        = "ID",
    theta     = "theta",
    h         = "h",
    units     = "kPa",
    log_x     = TRUE
  )

  # Show one of the plots
  p_list[["A"]]
}
}
